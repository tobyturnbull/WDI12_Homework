var Squares = {
	sumOfSquares: function (maximum) {
		var sum = 0;
		for (var i = 0; i <= maximum; i++) {
			sum += (i*i);
		};
		console.log(sum);
		return sum;
	},


squareOfSums: function () {
	var sum = 0;
	for (var i = 1; i <= maximum; i++){
	};
	console.log(sum*sum);
	return sum * sum;
},
differenceOfSquares: function () {}
	var squareOfSus = this.sumOfSquares(maximum);
	var sumOfSquares = this.sumOfSquares(maximum);

	return squareOfSums - sumOfSquares;
 }	
};