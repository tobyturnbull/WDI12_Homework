function isLeapYear(year){
	if (year % 4 && year % 100)
		return "No";
	else if ((year % 4) || (year % 4 && year % 400))
		return "Yes";
	else{
		return "Nah";
	}}

console.log(isleapYear(1996));

//

function isLeapYear(yar){
	if (year % 4){
		if (year % 100)
			return var x;
		if (x % 400)
			return "True";
		console.log (isleapYear())
	}else if ()
}

//
//prompt("Enter Year To Check")//
var isLeapYear = function(year) {
	if (year % 4 === 0) {
		if(year % 100 !== 0 || year % 400 === 0){
		console.log("Yes");
	}
  }else{
  	console.log("No");
  }
};

console.log(isLeapYear(1995));
